# Adjust user API token (Account Settings → "Your API token" in Adjust dashboard).
ADJUST_API_TOKEN=

# Shared password the UA team types to enter the dashboard.
DASHBOARD_PASSWORD=

# Optional: comma-separated Adjust app_tokens to restrict the dashboard to.
# Leave empty to fetch across all apps the API token has access to.
ADJUST_APP_TOKENS=
